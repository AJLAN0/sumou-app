/// Repository for projects.
///
/// Stub only in Sprint 1. The full API (list / details / create / assign
/// photographers / stages / closure requests) is defined in Sprint 2 alongside
/// `ProjectModel`. Declared now so dependency wiring and folder structure are
/// in place.
abstract interface class ProjectRepository {
  // Intentionally empty until Sprint 2.
}
